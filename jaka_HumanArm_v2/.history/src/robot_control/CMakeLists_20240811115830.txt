cmake_minimum_required(VERSION 3.8)
project(robot_control)

if(CMAKE_COMPILER_IS_GNUCXX OR CMAKE_CXX_COMPILER_ID MATCHES "Clang")
  add_compile_options(-Wall -Wextra -Wpedantic)
endif()

SET(CMAKE_BUILD_TYPE "Debug")


# 1
find_package(ament_cmake REQUIRED)

find_package(rclcpp REQUIRED)
find_package(moveit_core REQUIRED)
find_package(geometry_msgs REQUIRED)
find_package(moveit_ros_planning_interface REQUIRED)
find_package(moveit_common REQUIRED)
moveit_package()

find_package(Eigen3 REQUIRED)
find_package(Boost REQUIRED system filesystem date_time thread)
find_package(ament_cmake REQUIRED)
find_package(control_msgs REQUIRED)
find_package(moveit_core REQUIRED)
find_package(moveit_ros_planning REQUIRED)
find_package(moveit_ros_planning_interface REQUIRED)
find_package(moveit_ros_perception REQUIRED)
find_package(moveit_servo REQUIRED)
find_package(interactive_markers REQUIRED)
find_package(rviz_visual_tools REQUIRED)
find_package(moveit_visual_tools REQUIRED)
find_package(geometric_shapes REQUIRED)
find_package(rclcpp REQUIRED)
find_package(rclcpp_action REQUIRED)
find_package(pluginlib REQUIRED)
find_package(tf2_ros REQUIRED)
find_package(tf2_eigen REQUIRED)
find_package(tf2_geometry_msgs REQUIRED)

find_package(arm_hand_interface REQUIRED) # 接口功能包

set(THIS_PACKAGE_INCLUDE_DIRS
  doc/interactivity/include
)

set(THIS_PACKAGE_INCLUDE_DEPENDS
  ament_cmake
  rclcpp
  rclcpp_action
  tf2_geometry_msgs
  tf2_ros
  moveit_core
  rviz_visual_tools
  moveit_visual_tools
  moveit_ros_planning_interface
  interactive_markers
  tf2_geometry_msgs
  moveit_ros_planning
  pluginlib
  Eigen3
  Boost
  control_msgs
  moveit_servo
)

include_directories(${THIS_PACKAGE_INCLUDE_DIRS})

# 这个路径要根据实际情况进行设置，下面提供了两种添加方式
# INCLUDE_DIRECTORIES("/home/zj/ROS2_Work/jaka_HumanArm/src/robot_control/include")
INCLUDE_DIRECTORIES(${PROJECT_SOURCE_DIR}/include)

# 这个路径要根据实际情况进行设置，下面提供了两种添加方式
link_directories(${PROJECT_SOURCE_DIR}/lib)
#link_directories("/home/zj/ROS2_Work/jaka_HumanArm/src/robot_control/lib")





ament_export_dependencies(
  ${THIS_PACKAGE_INCLUDE_DEPENDS}
)
ament_export_include_directories(include)

# 2 需要修改的地方，robot_moveit_interface 和 robot_moveit_interface.cpp 替换为相应的代码
add_executable(robot_control src/robot_control.cpp  src/jaka.cpp)

ament_target_dependencies(
  robot_control
  "rclcpp"
  "rclcpp_action"
  "arm_hand_interface"
)

target_link_libraries(robot_control libjakaAPI.a)
#target_link_libraries(robot_control jakaAPI)



ament_target_dependencies(robot_control
 "arm_hand_interface"
  ${THIS_PACKAGE_INCLUDE_DEPENDS} Boost)

# 指定头文件搜索路径
# set(jakalib ${PROJECT_SOURCE_DIR}/libjakaAPI.a)
# target_include_directories(robot_control
# PUBLIC lib)
# 绝对引入 库文件目录
#link_libraries("${workspaceFolder}/src/robot_control/src")
#target_link_libraries(robot_control libjakaAPI)



install(TARGETS robot_control
  DESTINATION lib/${PROJECT_NAME}
)

install(DIRECTORY launch
  DESTINATION share/${PROJECT_NAME}
)


ament_package()
